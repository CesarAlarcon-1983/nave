mixin slider(klass, mode)    
    if mode == 'multi'
        ._slidermulti(class=klass)
            if block
                block
    if mode == 'thumbnail'
        ._thumbnails(class=klass)
            if block
                block
    if mode == 'normal'
        ._slider(class=klass)
            if block
                block
